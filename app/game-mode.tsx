import GameMode from '@screens/GameMode';

/**
 * Composant appelé par le router expo lorsque le path /game-mode est affiché
 * @constructor
 */
const GameModeScreen = () => {
  return <GameMode />;
};

export default GameModeScreen;
