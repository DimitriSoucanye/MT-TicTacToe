import SignIn from '@screens/SignIn';

/**
 * Composant appelé par le router expo lorsque le path /sign-in est affiché
 * @constructor
 */
const SignInScreen = () => {
  return <SignIn />;
};

export default SignInScreen;
