import React from 'react';
import { View } from 'react-native';

/**
 * Composant pour le screen Home
 * @constructor
 */
const Game = () => {
  return <View className={'bg-blue flex h-full'}></View>;
};

export default Game;
